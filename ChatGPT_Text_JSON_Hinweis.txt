ThunderCat v4.11 verwendet im ChatGPT-Modus standardmäßig Text/JSON.

Der vollständige Prompt wird direkt in Schritt 3 automatisch erzeugt und enthält
die aktuell gelesenen Absenderdaten. Deshalb gibt es keinen statischen Prompt,
der separat gepflegt werden muss.

Die CSV-Variante bleibt weiterhin als Alternative verfügbar.


Neu in v4.12:
- Button "Prompt kopieren"
- Button "Daten einfügen"
- beide Textfelder kompakter


Neu in v4.13:
- Nach erfolgreicher Klassifizierung führt ein eigener Button
  "Weiter zur Ordnerzuordnung" zu Schritt 4.


Neu in v4.14:
- E-Mail-Adressen aus ChatGPT-Antworten werden normalisiert.
- Markdown-Mailto-Links werden automatisch auf reine E-Mail-Adressen reduziert.
- Der Prompt verbietet Markdown-Links im Feld "email" ausdrücklich.
